Configuration:
  appenders:
    Console:
      name: STDOUT
      PatternLayout:
        Pattern: "%d %p [%t] [due %X{task.due}] [cur %X{current.time}] %C{1.} %m%n"
  Loggers:
    Root:
      level: '${sys:log4j.root.level:-DEBUG}'
      AppenderRef:
        - ref: STDOUT